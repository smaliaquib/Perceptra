
azure login
az group create --subscription "Azure General EXP" --name rg-ml-industrial-dev --location eastus --tags project=industrial environment=dev department=ml

az ml workspace create `
-name aml-industrial-ws `
--resource-group rg-ml-industrial-dev `
--tags project=industrial environment=dev department=ml `
--location eastus

$RG  = "rg-ml-industrial-dev"
$ACC = "amlindusstorage505231fa7"

$STORAGE_ACCOUNT_KEY = (az storage account keys list `
  --resource-group $RG `
  --account-name $ACC `
  --query "[0].value" -o tsv)

$STORAGE_ACCOUNT_KEY


# create container (if needed)
az storage container create `
  --name $CONTAINER `
  --account-name $ACC `
  --account-key $STORAGE_ACCOUNT_KEY

# upload
az storage blob upload `
  --container-name $CONTAINER `
  --file $FILEPATH `
  --name sample.txt `
  --account-name $ACC `
  --account-key $STORAGE_ACCOUNT_KEY